# Auto detect text files and perform LF normalization
* text=auto

# Shell scripts must keep LF endings regardless of platform - a CRLF
# shebang line breaks `curl | bash` and any direct execution on Linux.
*.sh text eol=lf
